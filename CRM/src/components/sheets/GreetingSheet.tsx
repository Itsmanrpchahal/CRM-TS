import React from "react";
import { MainWrapperWhite } from "../../utils/globalStyles";
import { withTheme } from "styled-components/native";

const GreetingSheet = () => {
    return (
        <MainWrapperWhite>

        </MainWrapperWhite>
    )
}

export default withTheme(GreetingSheet)